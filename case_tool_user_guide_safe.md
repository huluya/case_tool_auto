# 用例管理平台使用文档（脱敏版配置）

这是一份可对外分享的配置摘要，不包含真实数据库密码、账号密码或删除密码。

## 数据库配置

```dotenv
DB_HOST=<数据库地址>
DB_PORT=<数据库端口，默认 3306>
DB_USER=<数据库账号>
DB_PASSWORD=<数据库密码>
DB_NAME=<数据库名称>
BACKUP_DIR=<数据库备份目录>
```

数据库类型为 MySQL，字符集使用 `utf8mb4`。图片本体、用例、版本、列设置、合并关系和执行结果均保存在 MySQL 中。

## 应用配置

- 应用监听地址：`0.0.0.0:<应用端口>`
- 局域网访问地址：`http://<本机IP>:<应用端口>`
- 依赖安装：`pip install -r requirements.txt`
- 启动方式：`python app.py`

## 账号配置

| 账号 | 密码 | 权限 |
|---|---|---|
| `<管理员账号>` | `<管理员密码>` | 项目、版本、列设置和数据库备份 |
| `<测试账号>` | `<测试账号密码>` | 查看和执行测试 |

项目/版本删除密码：`<删除密码>`

## Excel 导出

选择项目和版本后，点击“下载当前版本 Excel”。系统会导出当前版本全部用例、当前可见列和排序、纵向合并关系、删除线文字、列宽及执行结果颜色。备注图片会直接以缩略图嵌入对应备注单元格。

## 备份与迁移

- `backups/case_manager_schema.sql`：只包含建库和建表结构，不包含业务数据。
- 数据库完整备份：使用管理员入口生成 SQL 快照，包含 MySQL 中的图片本体。
- 其他电脑部署：拉取代码、安装依赖、创建 `.env`、执行结构 SQL 或初始化数据库，然后启动应用。
